name: Build

on:
  push:
    branches:
      - branchMake

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build the program
        run: |
          make

