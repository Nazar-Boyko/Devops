name: Multi-Architecture Docker Build

on:
  push:
    branches:
      - branchHTTPserver

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image for amd64
      uses: docker/build-push-action@v4
      with:
        push: true
        tags: nazarboyko/http-server:latest-amd64
        platforms: linux/amd64

    - name: Build and push Docker image for arm64
      uses: docker/build-push-action@v4
      with:
        push: true
        tags: nazarboyko/http-server:latest-arm64
        platforms: linux/arm64

    - name: Create and push multi-architecture manifest
      run: |
        # Create the manifest list and push it
        docker manifest create nazarboyko/http-server:latest \
          nazarboyko/http-server:latest-amd64 \
          nazarboyko/http-server:latest-arm64

        docker manifest push nazarboyko/http-server:latest
